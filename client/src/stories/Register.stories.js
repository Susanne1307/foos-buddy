import React from 'react';
import RegisterForm from '../components/RegisterForm';

export default {
  title: 'Register',
  component: 'Register',
};

export const RegisterStory = () => <RegisterForm />;
